public class Main{
  public static void main (String [] args)
  {
      search_obj(10);
      obj.accept_array();
      int pos=obj.bin_search(21);
      if (pos!= -1)
        Sysout("found at "+pos + " position");
      else
        Sysout("not found");
  }

}

public int bin_search(int key)
{
  int low = 0;
  int high = arr.length-1;
  int mid = (low+high)/2;
  
  while((arr[mid]!=key) && ()low<=high))
  {
   if(key<arr[mid])
     











}